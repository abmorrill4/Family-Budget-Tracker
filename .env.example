DATABASE_URL=postgresql://postgres:postgres@localhost:5432/budget_tracker
PORT=3000
NODE_ENV=development

# YNAB Integration (optional)
# Generate a personal access token at: https://app.ynab.com/settings/developer
# The token is entered via the UI, not required as an env var.
